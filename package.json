{
  "name": "meridian-bridge",
  "version": "2.0.0",
  "description": "Meridian Bridge v2 — browser-native data pipeline & ETL canvas",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
